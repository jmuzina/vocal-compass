import { PitchAxis } from '../models/Axis/Pitch';
import { ResonanceAxis } from '../models/Axis/Resonance';

export const PITCH_AXIS = new PitchAxis();
export const RESONANCE_AXIS = new ResonanceAxis();

export const AXES = [PITCH_AXIS, RESONANCE_AXIS];
